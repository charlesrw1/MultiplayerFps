# CsRemake

A custom C++ game engine (VS2026, OpenGL, SDL2, vcpkg — no CMake).

## Stats

![C++ LOC over time](docs/images/loc_history.png)

`.cpp`/`.h` under `Source/`, excluding `Source/External/` (vendored third-party libs).
