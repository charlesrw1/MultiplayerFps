

// scene depth, normal
// write to blah

// constants
// probe_depth and probe_irradiance
// volume location, size, grid


layout(location = 0) out vec4 FS_OUT_Color;

#include "SharedGpuTypes.txt"

layout(binding = 0) uniform sampler2D ddgi_accum;		// ws normal (xyz)
layout(binding = 1) uniform sampler2D GBUFFER1;		// albedo
layout(binding = 2) uniform sampler2D ssao_tex;


layout (binding = 0, std140) uniform Ubo_View_Constant_Buffer {
	Ubo_View_Constants_Struct g;
};


#if 1
void main()
{
	vec2 uv = (vec2(gl_FragCoord.xy)) / vec2(g.viewport_size.xy);
	float ssao_val = texture(ssao_tex,uv).r;
	FS_OUT_Color = vec4(
		texture(ddgi_accum,uv).rgb*texture(GBUFFER1,uv).rgb/3.1415*ssao_val,
		1.0);
}
#endif
