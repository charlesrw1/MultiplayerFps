// Clear 256-bin histogram SSBO to zero.

layout(local_size_x = 256) in;

layout(std430, binding = 0) buffer HistogramSSBO {
    uint bins[256];
};

void main() {
    bins[gl_LocalInvocationIndex] = 0u;
}
